import 'package:flutter_test/flutter_test.dart';

void main() {
  // this is just to test codemagic stuff
  test(
    'simple test for testing 1 + 1 == 2',
    () async {
      expect(1 + 1 == 2, true);
    },
  );
}
