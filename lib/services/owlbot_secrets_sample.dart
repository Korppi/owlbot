/// Holds all secrets for OwlBot (only token)
///
/// Only this sample file is commited to git without real token.
/// After pull/fork you should copy this file with new name "owlbot_secrets.dart"
/// and add your token in there. That file is already gitignored.
class OwlBotSecrets {
  static final String token = '';
}
